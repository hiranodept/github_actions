name: Add comments when a new issue is created
on:
 issues:
   types: [opened]

jobs:
  add_comments:
    name: Add Comments
    if: github.event.label.name == 'new-issue'
    runs-on: ubuntu-latest
    steps:
     - run: |
          echo 'new-issue label was added'
